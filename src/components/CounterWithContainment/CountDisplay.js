import React from 'react';

export function CountDisplay ({count}) {
    return (
        <div style={{
            padding: "20px",
        }}>
            {count}
        </div>
    );
}
